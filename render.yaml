services:
  - type: web
    name: fastapi
    runtime: docker
    plan: free
    repo: https://github.com/atropass/ai-proj-backend
    region: Bahrain
    branch: main
    envVars:
      - key: MONGO_URL
        sync: false
    buildFilter:
      paths:
        - app/**
